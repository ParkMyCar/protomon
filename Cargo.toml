[package]
name = "protomon"
version = "0.1.0"
edition = "2024"

[dependencies]

[dev-dependencies]
criterion = "0.6"
proptest = { version = "1", features = ["attr-macro"] }

[[bench]]
name = "leb128"
harness = false
